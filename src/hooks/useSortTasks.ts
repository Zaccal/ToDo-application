import { useMemo } from 'react';
import { ToDoTask } from "../types/interfaces"

const useSortTasks = (tasks: ToDoTask[]): ToDoTask[] => {
    let sortedTasks;
    sortedTasks = useMemo(() => {

    }, [tasks])
}

export default useSortTasks